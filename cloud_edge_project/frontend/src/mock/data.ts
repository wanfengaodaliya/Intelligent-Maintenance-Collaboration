import type { DashboardMetrics, UnifiedTask, NetworkLink, EdgeNodeStatus, ModelUpdate, ServiceHealth } from '../types';

export const mockMetrics: DashboardMetrics = {
  total_packets: 2847,
  success_rate: 0.963,
  avg_latency_ms: 142.5,
  avg_total_latency_ms: 142.5,
  p95_latency_ms: 387.2,
  cloud_call_ratio: 0.28,
  edge_only_ratio: 0.55,
  weak_network_availability: 0.92,
  conflict_rate: 0.035,
  conflict_resolve_rate: 0.87,
  fallback_edge_ratio: 0.17,
  abnormal_ratio: 0.082,
};

export const mockTasks: UnifiedTask[] = Array.from({ length: 20 }, (_, i) => {
  const routes = ['edge', 'cloud', 'edge_cloud', 'fallback_edge'] as const;
  const route = routes[i % 4];
  const isSuccess = i % 7 !== 3;
  const isAbnormal = i % 5 === 0;
  return {
    id: `task_${1000 + i}`,
    source: `device_${(i % 3) + 1}`,
    route,
    label: isAbnormal ? 'abnormal' : 'normal',
    confidence: 0.85 + Math.random() * 0.14,
    risk_level: isAbnormal ? (i % 3 === 0 ? 'high' : 'medium') : 'low',
    total_latency_ms: 50 + Math.random() * 400,
    success: isSuccess,
    timestamp: new Date(Date.now() - i * 60000).toISOString(),
    scenario: 'bearing',
    recommendation_source: 'none' as const,
  };
});

export const mockNetworkLinks: NetworkLink[] = [
  {
    link_id: 'link_sender_1_edge_1',
    link_type: 'sender_to_edge',
    sender_id: 'sender_1',
    edge_id: 'edge_1',
    protocol: 'mqtt',
    proxy_name: 'toxiproxy_mqtt_1',
    listen: '0.0.0.0:1883',
    advertised_host: '127.0.0.1',
    advertised_port: 1883,
    upstream: 'edge_1:9001',
    current_state: 'normal',
    previous_state: 'normal',
    state_since_ns: Date.now() * 1_000_000 - 3600_000_000_000,
    applied_state_since_ns: Date.now() * 1_000_000 - 3600_000_000_000,
    seed: 42,
    desired_parameters: {
      state: 'normal',
      latency_ms: 5,
      jitter_ms: 1,
      bandwidth_kbps: 10000,
      packet_loss_percent: 0.1,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    applied_parameters: {
      state: 'normal',
      latency_ms: 5,
      jitter_ms: 1,
      bandwidth_kbps: 10000,
      packet_loss_percent: 0.1,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    link_reliability_score: 99.2,
    score_components: { latency: 98, uptime: 100, packet_loss: 99 },
    available: true,
    last_apply_success: true,
    last_apply_timestamp_ns: Date.now() * 1_000_000 - 600_000_000_000,
    consecutive_apply_failures: 0,
    error: null,
    report_enabled: true,
    latency_ms: 5,
    jitter_ms: 1,
    bandwidth_kbps: 10000,
    packet_loss_percent: 0.1,
  },
  {
    link_id: 'link_edge_1_scheduler',
    link_type: 'edge_to_scheduler',
    sender_id: null,
    edge_id: 'edge_1',
    protocol: 'http',
    proxy_name: 'toxiproxy_http_1',
    listen: '0.0.0.0:9001',
    advertised_host: '127.0.0.1',
    advertised_port: 9001,
    upstream: 'scheduler:8003',
    current_state: 'normal',
    previous_state: 'degraded',
    state_since_ns: Date.now() * 1_000_000 - 1800_000_000_000,
    applied_state_since_ns: Date.now() * 1_000_000 - 1800_000_000_000,
    seed: 17,
    desired_parameters: {
      state: 'normal',
      latency_ms: 10,
      jitter_ms: 2,
      bandwidth_kbps: 1000,
      packet_loss_percent: 0.5,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    applied_parameters: {
      state: 'normal',
      latency_ms: 15,
      jitter_ms: 3,
      bandwidth_kbps: 800,
      packet_loss_percent: 0.5,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    link_reliability_score: 95.5,
    score_components: { latency: 92, uptime: 98, packet_loss: 96 },
    available: true,
    last_apply_success: true,
    last_apply_timestamp_ns: Date.now() * 1_000_000 - 600_000_000_000,
    consecutive_apply_failures: 0,
    error: null,
    report_enabled: true,
    latency_ms: 12,
    jitter_ms: 2,
    bandwidth_kbps: 900,
    packet_loss_percent: 0.3,
  },
  {
    link_id: 'link_scheduler_cloud',
    link_type: 'scheduler_to_cloud',
    sender_id: null,
    edge_id: null,
    protocol: 'http',
    proxy_name: 'toxiproxy_http_2',
    listen: '0.0.0.0:9002',
    advertised_host: '127.0.0.1',
    advertised_port: 9002,
    upstream: 'cloud:8004',
    current_state: 'normal',
    previous_state: 'normal',
    state_since_ns: Date.now() * 1_000_000 - 7200_000_000_000,
    applied_state_since_ns: Date.now() * 1_000_000 - 7200_000_000_000,
    seed: 33,
    desired_parameters: {
      state: 'normal',
      latency_ms: 20,
      jitter_ms: 5,
      bandwidth_kbps: 500,
      packet_loss_percent: 0.2,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    applied_parameters: {
      state: 'normal',
      latency_ms: 18,
      jitter_ms: 4,
      bandwidth_kbps: 480,
      packet_loss_percent: 0.2,
      disconnect_mode: 'hard',
      packet_loss_applied: false,
    },
    link_reliability_score: 97.8,
    score_components: { latency: 95, uptime: 99, packet_loss: 98 },
    available: true,
    last_apply_success: true,
    last_apply_timestamp_ns: Date.now() * 1_000_000 - 600_000_000_000,
    consecutive_apply_failures: 0,
    error: null,
    report_enabled: true,
    latency_ms: 18,
    jitter_ms: 4,
    bandwidth_kbps: 480,
    packet_loss_percent: 0.2,
  },
];

export const mockEdgeNodeStatus: EdgeNodeStatus = {
  edge_node_id: 'edge_1',
  reported_at_ns: Date.now() * 1_000_000,
  resources: {
    logical_cpu_count: 8,
    cpu_utilization_percent: 45.2,
    memory_available_mb: 4096,
    gpu_available: true,
    npu_available: false,
    queue_length: 3,
  },
  models: [
    { model_version: 'v2.1.0', load_status: 'LOADED' },
    { model_version: 'v1.9.3', load_status: 'LOADED' },
  ],
  network_to_scheduler: {
    measured_at_ns: Date.now() * 1_000_000 - 5000_000_000,
    available_uplink_mbps_estimate: 95.5,
    rtt_ms_avg: 8.2,
    rtt_ms_p95: 12.5,
    loss_rate: 0.001,
  },
  last_task_activity_ns: Date.now() * 1_000_000 - 2000_000_000,
};

export const mockEdgeNode2Status: EdgeNodeStatus = {
  edge_node_id: 'edge_2',
  reported_at_ns: Date.now() * 1_000_000 - 20000_000_000,
  resources: {
    logical_cpu_count: 4,
    cpu_utilization_percent: 72.8,
    memory_available_mb: 1536,
    gpu_available: false,
    npu_available: true,
    queue_length: 8,
  },
  models: [
    { model_version: 'v2.0.5', load_status: 'LOADED' },
  ],
  network_to_scheduler: {
    measured_at_ns: Date.now() * 1_000_000 - 20000_000_000,
    available_uplink_mbps_estimate: 45.2,
    rtt_ms_avg: 25.1,
    rtt_ms_p95: 38.4,
    loss_rate: 0.05,
  },
  last_task_activity_ns: Date.now() * 1_000_000 - 30000_000_000,
};

export const mockModelUpdate: ModelUpdate = {
  update_id: 'mu_20260818_001',
  status: 'awaiting_confirmation',
  model_version: 'v2.2.0-rc1',
  created_at: '2026-08-18T10:00:00Z',
  dataset_packet_count: 1500,
  training_result: { accuracy: 0.972, f1_score: 0.968, precision: 0.965, recall: 0.971 },
  validation_result: { accuracy: 0.965, f1_score: 0.961 },
  confirmed_by: undefined,
};

export const mockServiceHealths: Record<string, ServiceHealth> = {
  log: { service: 'log_service', node_id: 'log_1', status: 'ok', port: 8006, model_backend: 'jsonl' },
  cloud: { service: 'cloud_service', node_id: 'cloud_01', status: 'ok', port: 8004, model_backend: 'mock' },
  edge: { service: 'edge_service', node_id: 'edge_1', status: 'ok', port: 8001, model_backend: 'moment_light_adapt', model_version: 'v2.1.0', model_deployment_status: 'LOADED', ready: true, mqtt_connected: true },
  scheduler: { service: 'scheduler', node_id: 'scheduler_1', status: 'ok', port: 8003 },
  network: { service: 'network_simulator', node_id: 'sim_1', status: 'ok', port: 8090, toxiproxy_available: true, scheduler_reporter_healthy: true, link_count: 3, available_link_count: 3, last_tick: 2847 },
};