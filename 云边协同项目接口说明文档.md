# 云边协同项目接口说明文档

> 适用对象：贾、彭、周、刘全体成员  
> 文档目的：统一模块之间的数据通信格式，避免后续出现“数据对不上、接口调不通、字段不一致”的问题。  
> 当前版本：V0.1 最小可运行版接口设计

---

## 1. 为什么要先定接口？

本项目由多个模块组成：

```text
周：任务生成器 / 数据仿真 / 网络仿真
贾：边缘模型服务 / 云端模型服务 / 系统集成
彭：调度器 / 雾节点 / DAG 与 PER-DDPG 算法
刘：日志记录 / 可视化页面 / 测试与文档
```

这些模块不是各自独立运行的，而是需要互相传递数据。例如：

```text
周生成任务 → 贾的边缘模型处理 → 彭的调度器决策 → 贾的云端模型处理 → 刘记录日志和展示指标
```

如果不提前约定接口，就会出现：

```text
周生成字段叫 temp，贾代码里读取 temperature；
贾输出字段叫 confidence，彭调度器里读取 score；
彭返回字段叫 path，刘前端里读取 route。
```

这样最后四个人的代码就很难合并。因此，接口的作用就是：

> **规定模块之间传什么数据、字段叫什么、类型是什么、返回什么结果。**

---

## 2. 什么是 `POST /cloud/infer` 这种写法？

接口通常写成：

```text
POST /cloud/infer
```

它由两部分组成：

```text
POST         请求方式
/cloud/infer 接口路径
```

完整地址一般是：

```text
http://服务器地址:端口/接口路径
```

例如云端模型服务运行在本机 8004 端口，则完整地址是：

```text
POST http://127.0.0.1:8004/cloud/infer
```

含义是：

> **向 127.0.0.1 这台机器的 8004 端口发送一条任务数据，让云端推理接口处理。**

注意：

```text
/cloud/infer 不是文件路径，不是 cloud 文件夹下面有 infer 文件。
```

它是后端程序定义出来的一个访问入口，例如 FastAPI 里可以这样写：

```python
from fastapi import FastAPI

app = FastAPI()

@app.post("/cloud/infer")
def cloud_infer(task: dict):
    return {
        "task_id": task["task_id"],
        "label": "abnormal",
        "confidence": 0.93
    }
```

意思是：

```text
当别人用 POST 方法访问 /cloud/infer 时，执行 cloud_infer 这个函数。
```

---

## 3. 请求方式说明

| 请求方式 | 通俗理解 | 本项目中的例子 |
|---|---|---|
| `GET` | 从后端获取数据 | 查询指标、查询任务列表、检查服务状态 |
| `POST` | 向后端发送数据，让后端处理 | 发送任务给边缘模型、发送任务给云端模型、提交日志 |
| `PUT` | 修改已有数据 | 本项目第一版暂时不用 |
| `DELETE` | 删除数据 | 本项目第一版暂时不用 |

本项目第一版主要使用：

```text
POST：提交任务、提交推理请求、提交日志
GET：查询状态、查询指标、查询任务列表
```

---

## 4. 最小主流程

第一版系统只需要先跑通下面这条主链路：

```text
周：任务生成器
    ↓ TaskRequest
贾：POST /edge/infer
    ↓ EdgeResult
彭：POST /scheduler/decide
    ↓ ScheduleDecision
如果 route = cloud
    ↓
贾：POST /cloud/infer
    ↓ CloudResult
刘：POST /logs/task_trace
    ↓
刘：GET /dashboard/metrics
```

对应大白话解释：

```text
1. 周生成一条模拟传感器任务；
2. 贾的边缘模型先做初步判断；
3. 彭的调度器判断任务留在边缘还是上传云端；
4. 如果需要上云，贾的云端模型进一步处理；
5. 刘的日志模块记录整个过程；
6. 刘的前端页面查询统计指标并展示。
```

---

# 5. 公共数据格式

公共数据格式是所有模块都必须遵守的数据模板。建议统一写在：

```text
common/schemas.py
examples/*.json
docs/api.md
```

> 注意：下面示例使用 `jsonc` 格式展示，里面包含 `// 注释`。正式接口传输 JSON 时，不能包含注释。

---

## 5.1 `TaskRequest`：任务输入格式

### 用途

用于表示一条系统任务。周的数据生成器、刘的页面、贾的边缘模型、彭的调度器都要使用这个格式。

### 主要流向

```text
周的数据生成器 / 刘的页面 → 贾的边缘节点服务
```

### 示例

```jsonc
{
  "task_id": "task_0001",              // 任务唯一编号，后续所有结果和日志都靠它关联
  "scenario": "industrial",            // 场景类型：industrial 表示工业场景；energy 表示能源场景
  "source_node": "edge_01",            // 任务来源节点，例如 edge_01、edge_02
  "task_type": "fault_detection",      // 任务类型，例如 fault_detection、energy_dispatch
  "timestamp": "2026-06-20 10:00:00",  // 任务产生时间
  "deadline_ms": 200,                  // 任务最大允许完成时间，单位毫秒
  "priority": 0.8,                     // 任务优先级，范围 0~1，越大越紧急
  "data_size_kb": 128,                 // 任务数据大小，单位 KB，用于估算传输时延
  "data": {                            // 具体业务数据，不同场景字段不同
    "device_id": "machine_01",         // 工业设备编号
    "temperature": 78.5,               // 温度
    "vibration": 0.63,                 // 振动强度
    "current": 13.2,                   // 电流
    "load": 0.76                       // 设备负载
  }
}
```

### 字段说明

| 字段 | 类型 | 是否必须 | 说明 |
|---|---|---:|---|
| `task_id` | string | 是 | 任务唯一编号 |
| `scenario` | string | 是 | 场景类型：`industrial` / `energy` |
| `source_node` | string | 是 | 任务来源节点 |
| `task_type` | string | 是 | 任务类型 |
| `timestamp` | string | 是 | 任务产生时间 |
| `deadline_ms` | int | 是 | 最大允许完成时延 |
| `priority` | float | 是 | 任务优先级，0~1 |
| `data_size_kb` | float | 是 | 数据大小 |
| `data` | object | 是 | 具体业务数据 |

---

## 5.2 `EdgeResult`：边缘模型输出格式

### 用途

边缘模型处理任务后，输出初步判断结果。这个结果会交给调度器判断是否上云，也会交给日志和前端展示。

### 主要流向

```text
贾的边缘模型服务 → 彭的调度器 / 刘的页面 / 日志模块
```

### 示例

```jsonc
{
  "task_id": "task_0001",              // 对应原始任务ID
  "node_id": "edge_01",                // 执行推理的边缘节点
  "model_name": "edge_small_model",    // 边缘模型名称或版本
  "label": "abnormal",                 // 边缘模型判断结果，例如 normal / abnormal
  "confidence": 0.72,                  // 边缘模型置信度，范围 0~1
  "risk_level": "medium",              // 风险等级：low / medium / high
  "edge_latency_ms": 38,               // 边缘模型推理耗时，单位毫秒
  "need_cloud": true                   // 边缘模型是否建议上云，最终决定仍由调度器做
}
```

### 字段说明

| 字段 | 类型 | 是否必须 | 说明 |
|---|---|---:|---|
| `task_id` | string | 是 | 对应原始任务ID |
| `node_id` | string | 是 | 执行推理的边缘节点 |
| `model_name` | string | 是 | 模型名称或版本 |
| `label` | string | 是 | 初步判断结果 |
| `confidence` | float | 是 | 置信度，0~1 |
| `risk_level` | string | 是 | 风险等级 |
| `edge_latency_ms` | float | 是 | 边缘推理耗时 |
| `need_cloud` | bool | 是 | 是否建议上云 |

---

## 5.3 `ScheduleDecision`：调度输出格式

### 用途

调度器根据任务信息、边缘结果、网络状态、节点状态，决定任务在哪里执行。

### 主要流向

```text
彭的调度器 → 贾的系统主流程 / 刘的页面
```

### 示例

```jsonc
{
  "task_id": "task_0001",              // 对应原始任务ID
  "route": "cloud",                    // 调度路径：edge / fog / cloud / fallback_edge / edge_cloud
  "target_node": "cloud_1",            // 目标执行节点
  "reason": "edge confidence is low and network is available", 
                                        // 调度原因，用于可解释展示
  "estimated_total_latency_ms": 145,   // 预计总时延，单位毫秒
  "upload_required": true              // 是否需要上传数据到云端或雾端
}
```

### `route` 取值说明

| route | 含义 |
|---|---|
| `edge` | 留在边缘本地执行 |
| `fog` | 交给雾节点执行 |
| `cloud` | 上传云端执行 |
| `fallback_edge` | 弱网或断网时边缘降级执行 |
| `edge_cloud` | 边缘和云端协同执行 |

### 字段说明

| 字段 | 类型 | 是否必须 | 说明 |
|---|---|---:|---|
| `task_id` | string | 是 | 对应原始任务ID |
| `route` | string | 是 | 调度路径 |
| `target_node` | string | 是 | 目标节点 |
| `reason` | string | 是 | 调度原因 |
| `estimated_total_latency_ms` | float | 是 | 预计总时延 |
| `upload_required` | bool | 是 | 是否需要上传数据 |

---

## 5.4 `CloudResult`：云端模型输出格式

### 用途

云端模型对复杂任务进行进一步推理，输出更高置信度或更完整的决策结果。

### 主要流向

```text
贾的云端模型服务 → 系统主流程 / 日志模块 / 刘的页面
```

### 示例

```jsonc
{
  "task_id": "task_0001",              // 对应原始任务ID
  "node_id": "cloud_1",                // 云端节点ID
  "model_name": "cloud_full_model",    // 云端模型名称或版本
  "label": "abnormal",                 // 云端判断结果
  "confidence": 0.93,                  // 云端置信度
  "risk_level": "high",                // 云端判断风险等级
  "cloud_latency_ms": 86,              // 云端推理耗时，单位毫秒
  "decision": {                        // 云端给出的最终建议
    "action": "send_alert",            // 执行动作，例如 send_alert / stop_machine / ignore
    "description": "设备存在高风险异常，建议停机检查"
  }
}
```

---

## 5.5 `TaskLog`：日志记录格式

### 用途

记录一条任务从产生到结束的完整过程。后续实验统计、前端展示、报告图表都依赖它。

### 主要流向

```text
边缘服务 / 调度器 / 云端服务 / 仲裁模块 → 日志模块
```

### 示例

```jsonc
{
  "task_id": "task_0001",              // 任务唯一编号
  "scenario": "industrial",            // 场景类型
  "source_node": "edge_01",            // 来源边缘节点
  "route": "cloud",                    // 实际调度路径
  "edge_latency_ms": 38,               // 边缘推理耗时
  "network_latency_ms": 30,            // 网络传输耗时
  "cloud_latency_ms": 86,              // 云端推理耗时，没有上云时可为 null
  "total_latency_ms": 154,             // 端到端总时延
  "edge_confidence": 0.72,             // 边缘模型置信度
  "cloud_confidence": 0.93,            // 云端模型置信度，没有上云时可为 null
  "success": true,                     // 任务是否成功完成
  "has_conflict": false,               // 是否发生决策冲突
  "conflict_resolved": null,           // 是否成功解决冲突，没有冲突时可为 null
  "timestamp": "2026-06-20 10:00:05"   // 日志记录时间
}
```

---

# 6. 核心接口定义

---

## 6.1 边缘推理接口：`POST /edge/infer`

### 调用关系

```text
周的数据生成器 / 刘的页面 → 贾的边缘模型服务
```

### 作用

边缘节点接收任务，调用边缘轻量模型，输出初步判断。

### 请求体

使用 `TaskRequest`。

### 响应体

使用 `EdgeResult`。

### 完整地址示例

本地单机模式：

```text
POST http://127.0.0.1:8001/edge/infer
```

多机或云端模式：

```text
POST http://边缘节点IP:8001/edge/infer
```

### Python 调用示例

```python
import requests

url = "http://127.0.0.1:8001/edge/infer"

task = {
    "task_id": "task_0001",
    "scenario": "industrial",
    "source_node": "edge_01",
    "task_type": "fault_detection",
    "timestamp": "2026-06-20 10:00:00",
    "deadline_ms": 200,
    "priority": 0.8,
    "data_size_kb": 128,
    "data": {
        "device_id": "machine_01",
        "temperature": 78.5,
        "vibration": 0.63,
        "current": 13.2,
        "load": 0.76
    }
}

response = requests.post(url, json=task)
print(response.json())
```

---

## 6.2 调度决策接口：`POST /scheduler/decide`

### 调用关系

```text
边缘节点 → 彭的调度器 / 雾节点
```

### 作用

根据任务信息、边缘推理结果、网络状态、节点状态，决定任务在哪里执行。

### 请求体示例

```jsonc
{
  "task": {                            // 原始任务的关键信息
    "task_id": "task_0001",            // 任务ID
    "scenario": "industrial",          // 场景类型
    "task_type": "fault_detection",    // 任务类型
    "deadline_ms": 200,                // 最大允许时延
    "priority": 0.8,                   // 优先级
    "data_size_kb": 128                // 数据大小
  },
  "edge_result": {                     // 边缘模型初步结果
    "label": "abnormal",               // 边缘判断结果
    "confidence": 0.72,                // 边缘置信度
    "edge_latency_ms": 38,             // 边缘推理耗时
    "need_cloud": true                 // 边缘模型是否建议上云
  },
  "network_state": {                   // 当前网络状态，由周的网络仿真模块提供
    "latency_ms": 30,                  // 当前网络延迟
    "bandwidth_mbps": 20,              // 当前带宽
    "packet_loss": 0.01,               // 丢包率
    "cloud_available": true            // 云端是否可达
  },
  "node_state": {                      // 当前节点资源状态
    "edge_cpu_usage": 0.55,            // 边缘CPU占用率
    "edge_memory_usage": 0.62,         // 边缘内存占用率
    "cloud_queue_length": 3,           // 云端排队任务数量
    "fog_available": true              // 雾节点是否可用
  }
}
```

### 响应体

使用 `ScheduleDecision`。

### 完整地址示例

```text
POST http://127.0.0.1:8003/scheduler/decide
```

---

## 6.3 云端推理接口：`POST /cloud/infer`

### 调用关系

```text
边缘节点 / 调度器 → 贾的云端模型服务
```

### 作用

当调度器决定上云时，由云端模型进行复杂推理或全局判断。

### 请求体示例

```jsonc
{
  "task_id": "task_0001",              // 任务ID
  "scenario": "industrial",            // 场景类型
  "task_type": "fault_detection",      // 任务类型
  "source_node": "edge_01",            // 来源节点
  "data": {                            // 原始业务数据或压缩后的特征
    "device_id": "machine_01",
    "temperature": 78.5,
    "vibration": 0.63,
    "current": 13.2,
    "load": 0.76
  },
  "edge_result": {                     // 边缘模型初判结果
    "label": "abnormal",
    "confidence": 0.72,
    "risk_level": "medium"
  }
}
```

### 响应体

使用 `CloudResult`。

### 完整地址示例

本地测试：

```text
POST http://127.0.0.1:8004/cloud/infer
```

云服务器部署：

```text
POST http://云服务器IP:8004/cloud/infer
```

---

## 6.4 冲突检测与仲裁接口：`POST /consistency/resolve`

### 调用关系

```text
多个边缘节点结果 / 云端结果 → 冲突仲裁模块
```

### 作用

检测多个节点决策是否冲突，并输出最终一致决策。主要用于能源场景。

### 请求体示例

```jsonc
{
  "decision_id": "decision_001",       // 一组待仲裁决策的编号
  "scenario": "energy",                // 场景类型
  "decisions": [                       // 多个节点给出的局部决策
    {
      "task_id": "task_A",             // 决策来源任务
      "source_node": "edge_01",        // 决策来源节点
      "target_device": "battery_01",   // 被控制的目标设备
      "action": "charge",              // 动作：charge 充电 / discharge 放电
      "power_kw": 80,                  // 请求功率
      "confidence": 0.85,              // 决策置信度
      "priority": 0.9,                 // 决策优先级
      "timestamp": "2026-06-20 10:00:01"
    },
    {
      "task_id": "task_B",
      "source_node": "edge_02",
      "target_device": "battery_01",
      "action": "discharge",
      "power_kw": 60,
      "confidence": 0.78,
      "priority": 0.7,
      "timestamp": "2026-06-20 10:00:02"
    }
  ],
  "global_constraints": {              // 全局约束条件
    "battery_01_max_power_kw": 100,    // 设备最大功率
    "allow_charge_and_discharge_same_time": false
                                        // 是否允许同一设备同时充放电
  }
}
```

### 响应体示例

```jsonc
{
  "decision_id": "decision_001",       // 对应仲裁编号
  "has_conflict": true,                // 是否存在冲突
  "conflict_type": "opposite_action",  // 冲突类型，例如 opposite_action / power_overload
  "final_action": "charge",            // 仲裁后的最终动作
  "selected_source_node": "edge_01",   // 被采纳的决策来源
  "reason": "charge decision has higher priority and confidence",
                                        // 仲裁理由
  "resolved": true                     // 冲突是否成功解决
}
```

### 完整地址示例

```text
POST http://127.0.0.1:8005/consistency/resolve
```

---

## 6.5 日志记录接口：`POST /logs/task_trace`

### 调用关系

```text
边缘服务 / 调度器 / 云端服务 / 仲裁模块 → 日志模块
```

### 作用

记录任务完整执行链路，用于后续实验统计和可视化。

### 请求体

使用 `TaskLog`。

### 响应体示例

```jsonc
{
  "task_id": "task_0001",              // 已记录的任务ID
  "saved": true,                       // 是否保存成功
  "log_path": "logs/task_trace.jsonl"  // 日志保存位置
}
```

### 完整地址示例

```text
POST http://127.0.0.1:8006/logs/task_trace
```

---

## 6.6 前端指标查询接口：`GET /dashboard/metrics`

### 调用关系

```text
刘的前端页面 → 日志模块 / 后端服务
```

### 作用

获取系统整体运行指标，用于页面展示和报告图表。

### 响应体示例

```jsonc
{
  "avg_latency_ms": 138,               // 平均端到端时延
  "p95_latency_ms": 186,               // P95 时延
  "cloud_call_ratio": 0.42,            // 云端调用比例
  "edge_only_ratio": 0.58,             // 仅边缘处理比例
  "weak_network_availability": 0.93,   // 弱网环境下业务保持率
  "conflict_rate": 0.04,               // 冲突发生比例
  "conflict_resolve_rate": 0.95,       // 冲突解决成功率
  "success_rate": 0.97                 // 任务成功率
}
```

### 完整地址示例

```text
GET http://127.0.0.1:8006/dashboard/metrics
```

---

## 6.7 前端任务查询接口：`GET /dashboard/tasks`

### 调用关系

```text
刘的前端页面 → 日志模块 / 后端服务
```

### 作用

获取最近任务列表，用于页面展示任务流。

### 响应体示例

```jsonc
{
  "tasks": [
    {
      "task_id": "task_0001",          // 任务ID
      "scenario": "industrial",        // 场景类型
      "source_node": "edge_01",        // 来源节点
      "route": "cloud",                // 调度路径
      "label": "abnormal",             // 最终判断结果
      "confidence": 0.93,              // 最终置信度
      "total_latency_ms": 154,         // 总时延
      "success": true,                 // 是否成功
      "timestamp": "2026-06-20 10:00:05"
    }
  ]
}
```

### 完整地址示例

```text
GET http://127.0.0.1:8006/dashboard/tasks
```

---

## 6.8 服务健康检查接口：`GET /health`

### 调用关系

```text
启动脚本 / 前端页面 / 测试程序 → 每个服务
```

### 作用

检查边缘、云端、调度器、日志服务是否正常启动。

### 响应体示例

```jsonc
{
  "service": "edge_service",           // 服务名称，例如 edge_service / cloud_service / scheduler_service
  "node_id": "edge_01",                // 节点ID
  "status": "ok",                      // 状态：ok / error
  "model_loaded": true,                // 模型是否加载成功
  "model_backend": "small",            // 模型模式：mock / small / full / remote
  "device": "cuda",                    // 推理设备：cpu / cuda
  "port": 8001                         // 当前服务端口
}
```

### 完整地址示例

```text
GET http://127.0.0.1:8001/health
GET http://127.0.0.1:8003/health
GET http://127.0.0.1:8004/health
GET http://127.0.0.1:8006/health
```

---

# 7. 推荐端口规划

| 服务 | 负责人 | 本地端口 | 主要接口 |
|---|---|---:|---|
| 边缘节点1 | 贾/周 | 8001 | `/edge/infer`, `/health` |
| 边缘节点2 | 贾/刘 | 8002 | `/edge/infer`, `/health` |
| 调度器/雾节点 | 彭 | 8003 | `/scheduler/decide`, `/health` |
| 云端模型服务 | 贾 | 8004 | `/cloud/infer`, `/health` |
| 冲突仲裁服务 | 周/彭 | 8005 | `/consistency/resolve`, `/health` |
| 日志与指标服务 | 刘 | 8006 | `/logs/task_trace`, `/dashboard/metrics`, `/dashboard/tasks`, `/health` |
| 可视化页面 | 刘 | 8501 | Streamlit / Gradio 页面 |

---

# 8. 接口优先级

## 第一优先级：先实现主流程

```text
POST /edge/infer
POST /scheduler/decide
POST /cloud/infer
GET /health
```

这四个接口跑通后，说明最小系统可以工作。

## 第二优先级：补实验统计和页面展示

```text
POST /logs/task_trace
GET /dashboard/metrics
GET /dashboard/tasks
```

这些接口用于刘后续做可视化和实验图表。

## 第三优先级：补第二场景和一致性

```text
POST /consistency/resolve
```

这个主要用于能源场景中的多节点冲突检测与仲裁。

---

# 9. 每个人应该关注什么

## 贾

重点关注：

```text
TaskRequest
EdgeResult
CloudResult
POST /edge/infer
POST /cloud/infer
GET /health
```

贾需要保证边缘模型和云端模型都能按照统一格式输入和输出。

---

## 彭

重点关注：

```text
TaskRequest
EdgeResult
ScheduleDecision
network_state
node_state
POST /scheduler/decide
```

彭需要明确调度器到底需要哪些字段，例如置信度、任务时延、网络延迟、带宽、节点负载等。

---

## 周

重点关注：

```text
TaskRequest
network_state
node_state
POST /consistency/resolve
```

周需要按照 `TaskRequest` 格式生成工业和能源任务，同时负责设计弱网、断网、冲突场景。

---

## 刘

重点关注：

```text
TaskLog
GET /dashboard/metrics
GET /dashboard/tasks
GET /health
```

刘需要负责接口文档、测试脚本、日志记录、指标展示和页面可视化。

---

# 10. 第一周建议执行顺序

## 第一步：贾起草 `common/schemas.py`

先定义以下结构：

```text
TaskRequest
EdgeResult
ScheduleDecision
CloudResult
TaskLog
```

## 第二步：刘整理 `docs/api.md`

把本文档中的接口整理进项目仓库，并维护示例 JSON。

## 第三步：周写 `examples/task_industrial.json`

先生成 5~10 条工业任务样例，格式必须符合 `TaskRequest`。

## 第四步：彭写规则调度器初版

先不做 PER-DDPG，先实现简单规则：

```text
如果 cloud_available = false → fallback_edge
如果 confidence < 0.8 且 cloud_available = true → cloud
如果 confidence >= 0.8 → edge
```

## 第五步：全员用 `/health` 和示例 JSON 测试接口

目标是第一周跑通：

```text
任务生成器 → 边缘推理 → 调度器 → 云端推理 → 日志记录
```

---

# 11. 最重要的约定

所有人必须遵守：

```text
1. 不要自己随便改字段名；
2. 不要把 IP、端口、模型路径写死在代码里；
3. 所有服务都必须有 /health；
4. 所有模块都必须用 TaskRequest 关联 task_id；
5. 调度路径 route 只能使用约定值；
6. 日志必须记录 total_latency_ms、route、success；
7. 正式传输 JSON 时不能带注释；
8. 字段要变更时必须先改 docs/api.md，再通知全员。
```

