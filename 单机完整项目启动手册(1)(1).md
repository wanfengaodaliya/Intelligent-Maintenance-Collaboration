# 云边协同智能维护项目：单机完整部署与启动手册（Windows）

本手册面向第一次拿到项目代码的同学。按照本文操作，可以在 **一台 Windows 电脑** 上完成环境配置、下载 MOMENT 模型，并启动完整系统。

```text
Sender（后续手动回放数据）
        ↓
Docker 网络模拟器（延迟、抖动、限速、断连）
        ↓
Edge 服务 ←→ Scheduler ←→ MOMENT 云端服务
```

项目所有 Python 服务统一使用一个 Conda 环境：`moment`：

| 环境 | 用途 | Python 与 NumPy |
|---|---|---|
| `moment` | Edge、Scheduler、Cloud、Sender | Python 3.11.15，NumPy 1.25.2 |

不要再创建 `edge-runtime` 或项目 `.venv`。网络模拟器由 Docker 独立运行，不需要 Python 环境。

> 本文统一使用 Conda 的 `moment` 环境；不要创建项目 `.venv` 或第二套运行环境。

---

## 1. 前置软件

请先安装：

1. Git；
2. Docker Desktop；
3. 以下二选一：Miniconda/Anaconda，或 Python 3.11.15。

如需使用 NVIDIA 显卡运行 MOMENT，还需要正确安装显卡驱动。没有显卡时项目仍能用 CPU 运行，只是云端推理较慢。

打开 Docker Desktop，等待其启动完成。随后在 PowerShell 执行：

```powershell
docker info
```

能看到 Docker 的 `Server` 信息即可继续。

---

## 2. 获取最新代码

在 PowerShell 中进入准备存放项目的目录，例如：

```powershell
cd D:\your\path
```

克隆项目：

```powershell
git clone https://github.com/wanfengaodaliya/Intelligent-Maintenance-Collaboration.git
```

进入项目：

```powershell
cd <项目根目录>
```

切换并更新主分支：

```powershell
git switch main
```

```powershell
git pull --ff-only
```

本文后续命令均以项目根目录为基准，例如：

```text
<项目根目录>\Intelligent-Maintenance-Collaboration
```

如果你的项目目录不同，请替换命令中的该路径。

---

## 3. 创建统一 Python 环境

创建统一 `moment` 环境：

```powershell
conda create -n moment python=3.11.15 -y
```

激活并安装项目统一依赖：

```powershell
conda activate moment
cd <项目根目录>\cloud_edge_project
python -m pip install --upgrade pip
python -m pip install -r requirements-moment.txt
python -m pip check
```

---

## 4. 配置 MOMENT 云端环境

### 4.1 激活环境

```powershell
conda activate moment
```
命令行左侧应显示 `(moment)`。

### 4.2 安装依赖

进入项目根目录：

```powershell
cd <项目根目录>
```

```powershell
python -m pip install --upgrade pip
```

```powershell
python -m pip install -r cloud_edge_project\requirements-moment.txt
```

上述命令中的 `momentfm` 不从 PyPI 下载，而是使用 Git 从 MOMENT 官方源码仓库安装，并固定到本项目验证过的提交 `38f7310ad594100747ca2a8357e9c7ca7d323e0e`。因此，执行前必须已安装 Git；不需要单独克隆 MOMENT 仓库。

检查依赖：

```powershell
python -m pip check
```

检查关键版本和 CUDA：

```powershell
python -c "import torch, transformers, numpy; from importlib.metadata import version; print('torch =', torch.__version__); print('cuda =', torch.cuda.is_available()); print('transformers =', transformers.__version__); print('momentfm =', version('momentfm')); print('numpy =', numpy.__version__)"
```

必须确认：

```text
torch = 2.13.0（已安装 CUDA 构建时可能显示为 2.13.0+cu130）
transformers = 5.15.0
momentfm = 0.1.5（官方源码仓库固定提交安装）
numpy = 1.25.2
```

`cuda = True` 表示可使用显卡；`False` 表示服务将自动使用 CPU。

---

## 5. 下载 MOMENT 模型

保持 `moment` 环境激活。模型公开存放在 Hugging Face：

<https://huggingface.co/wanfengaodaliya/moment>

模型为公开仓库，下载不需要登录 Hugging Face。

安装下载工具：

```powershell
pip install -U "huggingface_hub[cli]"
```

进入 `cloud_edge_project` 目录。运行资产统一放在该目录下的 `model_assets\moment`，不再使用 `local_experiment`、`experiments` 或 Junction：

```powershell
cd <项目根目录>\cloud_edge_project
```

下载模型。Hugging Face 仓库默认分支只保留运行所需的 MOMENT 资产，命令会直接写入项目标准运行目录，无需复制或迁移：

```powershell
hf download wanfengaodaliya/moment --local-dir model_assets\moment
```

`model_assets` 被 Git 忽略，模型权重不会进入代码提交；换新电脑或重新检出项目后需要重新执行本节。下载目录会包含少量 `.cache\huggingface` 元数据，但不会下载实验结果。历史实验资产保留在 Hugging Face 的 `legacy-experiments` 分支。不要自行创建指向父目录的 Junction。

下载完成后，以下文件必须存在：

```text
model_assets\moment\releases\moment-scl05-final\best_model.pt
model_assets\moment\releases\moment-scl05-final\condition_norm.json
model_assets\moment\releases\moment-scl05-final\moment_model.py
model_assets\moment\pretrained\MOMENT-1-small\config.json
model_assets\moment\pretrained\MOMENT-1-small\model.safetensors
```

验证关键文件：

```powershell
$requiredMomentFiles = @(
    "model_assets\moment\releases\moment-scl05-final\best_model.pt",
    "model_assets\moment\releases\moment-scl05-final\condition_norm.json",
    "model_assets\moment\releases\moment-scl05-final\moment_model.py",
    "model_assets\moment\pretrained\MOMENT-1-small\config.json",
    "model_assets\moment\pretrained\MOMENT-1-small\model.safetensors"
)
$requiredMomentFiles | ForEach-Object {
    [PSCustomObject]@{ Path = $_; Exists = Test-Path -LiteralPath $_ }
}
```

五个文件都显示 `Exists = True` 后才继续。

---

## 6. 部署大语言模型（llama.cpp + Qwen2.5）

本节部署两个独立的 LLM 服务，共用同一个 llama.cpp 安装与模型目录：

| 服务 | 模型 | 端口 | 用途 |
|---|---|---|---|
| 边缘建议 LLM | Qwen2.5-0.5B-Instruct（Q3_K_M） | `127.0.0.1:8005` | 设备级维护建议（≤30 字） |
| 云端模型更新 LLM | Qwen2.5-3B-Instruct（Q4_K_M） | `127.0.0.1:6006` | 模型更新建议书（较长文本） |

- **边缘建议 LLM**：Edge 向前端推送维护建议时，规则引擎先决定建议的类型与优先级，再由本机大语言模型把结构化结果翻译成一句不超过 30 字的中文建议。
- **云端模型更新 LLM**：全局分析发现模型问题、规则判定需要创建模型更新任务时，由云端调用该服务生成一份面向人工审核的自然语言《模型更新建议书》（更新什么、为什么、怎么更新）。独立于推理后端（MOMENT），挂了自动回退模板，不影响更新闭环。

两者都提供 OpenAI 兼容接口，模型文件来自官方公开仓库，未做任何修改，下载不需要登录。任一个服务未启动，对应链路文案自动退化为固定模板，系统其余功能不受影响；建议按本节完成全部部署。llama.cpp 约 530MB，0.5B 模型约 400MB，3B 模型约 1.9GB。

### 6.1 下载 llama.cpp

统一下载 CUDA 版（NVIDIA 显卡通用），版本固定为 b6240。

安装位置：统一安装到 `D:\develop\llama.cpp`（命令会自动创建该目录，无需手动新建；程序与模型合计约 1GB，请确保所在磁盘有足够空余空间）。选择项目目录之外的位置，是为了避免将近 1GB 的文件混入 Git 仓库；全队使用同一约定路径，出问题时也便于互相排查。电脑没有 D 盘时，可整体替换为其他路径（如 `C:\develop\llama.cpp`），并同步替换 6.2、6.3、6.4、6.5 节及 `start_project.ps1` 中出现的该路径。

把下面整段复制到 PowerShell 执行（使用 `curl.exe` 下载，避免 PowerShell 5.1 的 `Invoke-WebRequest` 在部分电脑上的兼容问题）：

```powershell
# 1. 创建安装目录
$dir = "D:\develop\llama.cpp"
New-Item -ItemType Directory -Force -Path $dir | Out-Null

# 2. 下载主程序包和显卡运行库（两个都要，缺一不可，共约 530MB）
curl.exe -L -k -o "$env:TEMP\llama-main.zip" "https://github.com/ggml-org/llama.cpp/releases/download/b6240/llama-b6240-bin-win-cuda-12.4-x64.zip"
curl.exe -L -k -o "$env:TEMP\llama-cudart.zip" "https://github.com/ggml-org/llama.cpp/releases/download/b6240/cudart-llama-bin-win-cuda-12.4-x64.zip"

# 3. 解压到同一目录，清理安装包
Expand-Archive "$env:TEMP\llama-main.zip" -DestinationPath $dir -Force
Expand-Archive "$env:TEMP\llama-cudart.zip" -DestinationPath $dir -Force
Remove-Item "$env:TEMP\llama-main.zip", "$env:TEMP\llama-cudart.zip"
```

验证：

```powershell
Test-Path D:\develop\llama.cpp\llama-server.exe
```

返回 `True` 后继续。

说明：

- 两个压缩包必须解压到同一个目录，少任何一个 `llama-server.exe` 都无法启动；
- 命令下载失败时，用浏览器打开 <https://github.com/ggml-org/llama.cpp/releases/tag/b6240>，手动下载上面两个同名 zip，解压到安装目录。

### 6.2 下载 Qwen2.5-0.5B 模型文件

模型文件约 400MB，从国内镜像下载（`hf-mirror.com`），避免 Hugging Face 主站在国内的访问问题。直接用 `curl.exe` 下载，无需额外安装工具：

```powershell
# 创建模型目录
New-Item -ItemType Directory -Force -Path "D:\develop\llama.cpp\models" | Out-Null

# 下载模型文件（国内镜像，-L 跟随重定向，-k 跳过证书验证）
curl.exe -L -k -o "D:\develop\llama.cpp\models\qwen2.5-0.5b-instruct-q3_k_m.gguf" "https://hf-mirror.com/Qwen/Qwen2.5-0.5B-Instruct-GGUF/resolve/main/qwen2.5-0.5b-instruct-q3_k_m.gguf"
```

如果 `hf-mirror.com` 访问困难，可改用 Hugging Face 主站（去掉 URL 中的 `hf-mirror.com`，换成 `huggingface.co`），同样加 `-L -k` 参数。

验证：

```powershell
Test-Path D:\develop\llama.cpp\models\qwen2.5-0.5b-instruct-q3_k_m.gguf
```

返回 `True` 后继续。

### 6.3 下载云端 3B 模型文件

模型文件约 1.9GB，从国内镜像下载（`hf-mirror.com`），方式与 6.2 一致：

```powershell
# 模型目录已由 6.2 创建过，此处确保存在
New-Item -ItemType Directory -Force -Path "D:\develop\llama.cpp\models" | Out-Null

# 下载 3B 模型（Q4_K_M 量化，与 0.5B 的 Q3_K_M 区分开）
curl.exe -L -k -o "D:\develop\llama.cpp\models\qwen2.5-3b-instruct-q4_k_m.gguf" "https://hf-mirror.com/Qwen/Qwen2.5-3B-Instruct-GGUF/resolve/main/qwen2.5-3b-instruct-q4_k_m.gguf"
```

如果 `hf-mirror.com` 访问困难，可改用主站（去掉 URL 中的 `hf-mirror.com`，换成 `huggingface.co`），同样加 `-L -k` 参数。

验证：

```powershell
Test-Path D:\develop\llama.cpp\models\qwen2.5-3b-instruct-q4_k_m.gguf
```

返回 `True` 后继续。

### 6.4 启动边缘建议 LLM 服务（0.5B @ 8005）

新开一个 PowerShell 窗口。该窗口与 Scheduler、Cloud、Edge 一样需要保持运行。注意端口为 **8005**（非旧的 8002——8002 已被 edge_02 服务占用，`start_project.ps1` 及云端/边缘环境变量均指向 8005）。

```powershell
cd D:\develop\llama.cpp
.\llama-server.exe --model .\models\qwen2.5-0.5b-instruct-q3_k_m.gguf --host 127.0.0.1 --port 8005 --ctx-size 2048 --n-gpu-layers 99
```

`--n-gpu-layers 99` 表示把模型完整放进显存，GTX 1650 Ti（4GB 显存）及以上均够用。日志出现 `listening` 字样即启动成功。

若启动时报 CUDA 或 GPU 相关错误（常见于显卡驱动过旧），去掉 `--n-gpu-layers 99` 参数改用 CPU 运行即可，速度同样满足要求。

### 6.5 启动云端模型更新 LLM 服务（3B @ 6006）

新开一个 PowerShell 窗口，与上面的边缘 LLM 并行运行（这是系统的第 6 个服务窗口，需要保持运行）：

```powershell
cd D:\develop\llama.cpp
.\llama-server.exe --model .\models\qwen2.5-3b-instruct-q4_k_m.gguf --host 127.0.0.1 --port 6006 --ctx-size 4096 --n-gpu-layers 99
```

说明：

- `--port 6006` 与云端 `VLLM_URL` 的默认端口一致，无需改代码或环境变量；
- `--ctx-size 4096` 大于边缘 0.5B 的 2048，用于支撑建议书的较长生成（`max_tokens=512`）；
- 显存占用约 1.9GB（0.5B 另占约 0.4GB，合计约 2.3GB），GTX 1650 Ti（4GB）够用；若不够可去掉 `--n-gpu-layers 99` 改 CPU。

### 6.6 验证边缘建议 LLM（0.5B）

保持 6.4 的窗口运行，另开一个 PowerShell 窗口执行：

```powershell
curl.exe -s http://127.0.0.1:8005/v1/chat/completions -H "Content-Type: application/json" -d '{\"messages\":[{\"role\":\"user\",\"content\":\"只输出一句中文建议，不超过30个字。设备ID:dev_01，状态:故障，置信度:0.92，建议类型:停机检修。\"}],\"max_tokens\":32,\"temperature\":0.1,\"stop\":[\"。\",\"\\n\"]}'
```

返回 JSON 中 `choices[0].message.content` 为一句通顺的中文建议即部署成功。首次调用因 GPU 预热可能较慢（约数十秒），后续调用会稳定在毫秒级。

也可以运行项目自带的端到端验证脚本（规则引擎 + LLM 全链路）：

```powershell
conda activate moment
cd <项目根目录>\cloud_edge_project
python edge_service\verification\test_suggestion_llm.py
```

输出中每条建议标注 `[OK]` 表示 LLM 生成成功；`[FALLBACK]` 表示 LLM 服务未连上，已退化为固定模板。

### 6.7 验证云端模型更新 LLM（3B）

保持 6.5 的窗口运行，另开 PowerShell 执行：

```powershell
curl.exe -s http://127.0.0.1:6006/v1/chat/completions -H "Content-Type: application/json" -d '{\"messages\":[{\"role\":\"user\",\"content\":\"用30个字以内说明为什么要更新模型。\"}],\"max_tokens\":512,\"temperature\":0.1}'
```

返回 JSON 中 `choices[0].message.content` 有一段通顺的中文说明即部署成功。

### 6.8 云端环境变量（可选，默认已匹配）

云端 `llm_client` 走 OpenAI 兼容接口，默认值已与 6.5 的启动参数一致，通常无需配置；如需覆盖可设置：

| 环境变量 | 默认值 | 说明 |
|---|---|---|
| `VLLM_URL` | `http://127.0.0.1:6006/v1/chat/completions` | 云端模型更新 LLM 地址 |
| `VLLM_MODEL_NAME` | `qwen-cloud` | 单模型服务下该字段被忽略，可留默认 |
| `VLLM_API_KEY` | 空 | llama.cpp 无需鉴权，留空 |
| `VLLM_TIMEOUT_SECONDS` | `120` | 建议书生成超时（秒） |

---

## 7. 配置正式 Distilled H5 Edge

Edge 与 Scheduler、Cloud、Sender 统一使用已经创建好的 `moment` 环境。RF、`edge-runtime` 和旧的 Edge 依赖清单不再使用。

### 7.1 H5 模型目录与配置

H5 运行时从项目目录加载模型，不引用任何开发者本机的绝对路径：

```text
<项目根目录>\cloud_edge_project\edge_service\models\distilled_h5
```

该目录使用版本化制品结构。先读取 `active_version.json` 的 `version` 字段，
再从对应的版本子目录加载模型，例如：

```text
distilled_h5\active_version.json
distilled_h5\distilled_h5_kd_fold3_a9f20442\best_model.pt
```

如果该目录缺少 H5 文件，在统一 `moment` 环境中从公开 Hugging Face 仓库恢复：

```powershell
cd <项目根目录>
hf download wanfengaodaliya/intelligent-maintenance-distilled-h5 --local-dir cloud_edge_project\edge_service\models\distilled_h5
```

H5 checkpoint 的 SHA256 必须校验：

```powershell
$modelDir = "cloud_edge_project\edge_service\models\distilled_h5"
$version = ((Get-Content "$modelDir\active_version.json" -Raw | ConvertFrom-Json).version).Trim()
$versionDir = Join-Path $modelDir $version
$expected = (Get-Content "$versionDir\checkpoint_sha256.txt" -Raw).Trim()
$actual = (Get-FileHash "$versionDir\best_model.pt" -Algorithm SHA256).Hash.ToLower()
if ($actual -eq $expected) { 'H5 checkpoint hash: OK' } else { throw 'H5 checkpoint hash mismatch' }
```

cmd.exe：

```bat
cd /d <项目根目录>
powershell -NoProfile -Command "$m='cloud_edge_project\edge_service\models\distilled_h5'; $v=((Get-Content (Join-Path $m 'active_version.json') -Raw | ConvertFrom-Json).version).Trim(); $d=Join-Path $m $v; if (-not (Test-Path (Join-Path $d 'best_model.pt'))) { throw 'H5 checkpoint missing' }; (Get-FileHash (Join-Path $d 'best_model.pt') -Algorithm SHA256).Hash; Get-Content (Join-Path $d 'checkpoint_sha256.txt')"
```

打开：

```text
<项目根目录>\cloud_edge_project\configs\local.yaml
```

确认：

```yaml
model:
  edge_backend: "local_h5"

mqtt:
  host: "127.0.0.1"
  port: 1883
  input_topic: "edge/edge_01/input"
  client_id: "edge_01-runtime"
```

`edge_backend` 不能改成 RF、rule 或其他旧后端。H5 对每个原始 50 ms 包独立推理，不进行 20 包前置合并。

---

## 8. 一键启动脚本（推荐）

完成第 1 至第 7 节的环境与模型部署后，使用项目根目录下的 `start_project.ps1` 可以一条命令启动全套服务，自动弹出 5 个 PowerShell 窗口并完成健康检查。

### 8.1 基本用法

打开 PowerShell，进入项目根目录，执行：

```powershell
cd <项目根目录>
.\start_project.ps1
```

脚本会依次：

1. **前置检查**：Docker 是否运行、`moment` 环境是否存在、MOMENT / H5 / 边缘 0.5B / 云端 3B 模型文件是否就位，缺任何一项会报错退出
2. **启动网络模拟器**：Docker Compose 拉起 Toxiproxy 容器
3. **自动弹出 6 个 PowerShell 窗口**：Scheduler（8003）、MOMENT 云端（8004）、Edge edge_01（8001）、Edge edge_02（8002）、边缘建议 LLM（8005）、云端模型更新 LLM（6006），各窗口独立运行互不干扰
4. **等待 20 秒后自动做健康检查**：每个服务的 `/health` 接口都会被检测并打印结果

看到 `========== Done ==========` 即表示启动完成，可以开始回放 Sender 数据。

### 8.2 可选参数

| 参数 | 说明 |
|---|---|
| `-ProjectRoot "D:\其他路径"` | 项目不在脚本所在目录时，手动指定项目根路径 |
| `-SkipLLM` | 跳过第 5、第 6 个 LLM 窗口（边缘建议与云端模型更新同时跳过）。系统仍可完整运行，仅建议文案与建议书退化为固定模板 |

示例：

```powershell
# 跳过 LLM，快速启动核心链路
.\start_project.ps1 -SkipLLM

# 项目在其他位置
.\start_project.ps1 -ProjectRoot "D:\work\IMC"
```

### 8.3 停止项目

关闭所有弹出的 PowerShell 窗口（或在每个窗口中按 `Ctrl+C`），然后在项目根目录执行：

```powershell
cd <项目根目录>\cloud_edge_project\internet_service\network_simulator
docker compose --env-file .env down
```

该操作只停止服务与容器，不会删除 Conda 环境、项目代码或已下载的模型。

---

## 9. 启动完整系统（手动方式）

如果不想用一键脚本，也可以按下面步骤手动启动。使用六个 PowerShell 窗口（第 5、6 个可选），并严格按照下面顺序启动：

```text
窗口 1：Docker 网络模拟器
    ↓
窗口 2：Scheduler
    ↓
窗口 3：MOMENT 云端服务
    ↓
窗口 4：Edge 服务
    ↓
窗口 5：边缘建议 LLM 服务（可选）
    ↓
窗口 6：云端模型更新 LLM 服务（可选）
```

### 9.1 窗口 1：启动 Docker 网络模拟器

打开 Docker Desktop 后，新开终端：

```powershell
cd <项目根目录>\cloud_edge_project\internet_service\network_simulator
```

第一次运行时，创建本机网络配置：

```powershell
if (-not (Test-Path .env)) { Copy-Item .env.example .env }
```

若 `.env` 已经存在，不要再次执行复制命令。

启动网络模拟器：

```powershell
docker compose --env-file .env up -d --build
```

等待十几秒后检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8090/health
```

应确认：

```text
status              : ok
toxiproxy_available : True
link_count          : 18
```

此时 Scheduler 尚未启动，`scheduler_reporter_healthy` 可能暂时为 `False`，或出现网络上报失败日志；这是正常现象，Scheduler 启动后会自动恢复。

### 9.2 窗口 2：启动 Scheduler

新开 PowerShell，并激活 `moment` 环境。

Conda：

```powershell
conda activate moment
```

进入服务目录：

```powershell
cd <项目根目录>\cloud_edge_project
```

让 Scheduler 通过网络模拟器向 Edge 下发任务：

```powershell
$env:SCHEDULER_EDGE_NODES_JSON = '{"edge_01":{"control_url":"http://127.0.0.1:18042","target_topic":"edge/edge_01/input"}}'
```

启动 Scheduler：

```powershell
python -m uvicorn scheduler.api:app --host 127.0.0.1 --port 8003
```

### 9.3 窗口 3：启动 MOMENT 云端服务

新开 PowerShell，并激活 `moment` 环境。

Conda：

```powershell
conda activate moment
```

进入服务目录：

```powershell
cd <项目根目录>\cloud_edge_project
```

设置云端模型与代理地址：

```powershell
$env:CLOUD_BACKEND = "moment_light_adapt"
```

```powershell
$env:CLOUD_MOMENT_DEVICE = "auto"
```

```powershell
$env:SCHEDULER_SERVICE_BASE_URL = "http://127.0.0.1:18045"
```

启动云端服务：

```powershell
python -m uvicorn cloud_service.app:app --host 127.0.0.1 --port 8004
```

### 9.4 窗口 4：启动 Edge H5 服务

新开终端，激活统一的 `moment` 环境。Edge 的 H5 模型目录和 `local.yaml` 配置见第 7.1 节；节点 ID、代理地址和启动命令在本节直接设置。不要切换到 `edge-runtime`。

PowerShell：

```powershell
conda activate moment
cd <项目根目录>\cloud_edge_project
$env:EDGE_NODE_ID = "edge_01"
$env:SCHEDULER_SERVICE_BASE_URL = "http://127.0.0.1:18011"
$env:CLOUD_SERVICE_BASE_URL = "http://127.0.0.1:18021"
python edge_service/run_edge_service.py --host 127.0.0.1 --port 8001
```

cmd.exe：

```bat
conda activate moment
cd /d <项目根目录>\cloud_edge_project
set EDGE_NODE_ID=edge_01
set SCHEDULER_SERVICE_BASE_URL=http://127.0.0.1:18011
set CLOUD_SERVICE_BASE_URL=http://127.0.0.1:18021
python edge_service\run_edge_service.py --host 127.0.0.1 --port 8001
```

网络模拟器会动态改变链路质量。链路处于 `BAD` 或 `DISCONNECTED` 时，Edge 日志可能出现 `ReadTimeout` 或 `ConnectionError`；这表示网络模拟正在生效，不等于 Edge 服务停止。

### 9.5 窗口 5：启动建议 LLM 服务（可选）

若已按第 6.4 节部署边缘建议大语言模型，新开一个 PowerShell 窗口：

```powershell
cd D:\develop\llama.cpp
.\llama-server.exe --model .\models\qwen2.5-0.5b-instruct-q3_k_m.gguf --host 127.0.0.1 --port 8005 --ctx-size 2048 --n-gpu-layers 99
```

完整说明见第 6.4 节。该窗口需要保持运行；不启动时系统仍可完整运行，仅建议文案退化为固定模板。

### 9.6 窗口 6：启动云端模型更新 LLM 服务（可选）

若已按第 6.5 节部署云端 3B 大语言模型，再开一个 PowerShell 窗口：

```powershell
cd D:\develop\llama.cpp
.\llama-server.exe --model .\models\qwen2.5-3b-instruct-q4_k_m.gguf --host 127.0.0.1 --port 6006 --ctx-size 4096 --n-gpu-layers 99
```

完整说明见第 6.5 节。该窗口需要保持运行；不启动时，模型更新建议书退化为固定模板，但模型更新闭环（规则判定 → 数据准备 → 训练 → 审核 → 分发）不受影响。

---

## 10. 健康检查：确认系统可用

在新的 PowerShell 窗口中依次执行：

```powershell
Invoke-RestMethod http://127.0.0.1:8090/health
```

```powershell
Invoke-RestMethod http://127.0.0.1:8003/health
```

```powershell
Invoke-RestMethod http://127.0.0.1:8004/health
```

```powershell
Invoke-RestMethod http://127.0.0.1:8001/health
```

必须满足：

| 服务 | 成功标志 |
|---|---|
| 网络模拟器 | `status : ok`、`link_count : 18` |
| Scheduler | `service : scheduler_service`、`status : ok` |
| MOMENT 云端 | `status : ok`、`model_backend : moment_light_adapt` |
| Edge | `status : ok`、`node_id : edge_01`、`mqtt_connected : True` |

若部署了第 6.4 节的边缘建议 LLM 服务，可额外检查（可选）：

```powershell
Invoke-RestMethod http://127.0.0.1:8005/v1/models
```

若部署了第 6.5 节的云端模型更新 LLM 服务，也可额外检查（可选）：

```powershell
Invoke-RestMethod http://127.0.0.1:6006/v1/models
```

两者能返回模型信息即可。未启动任一 LLM 服务均不影响系统运行，仅对应的建议文案 / 建议书退化为固定模板。

全部通过后，完整系统已启动，可以再进行 Sender 的 MAT 数据回放。

---

## 11. 网络代理说明

单机模式只使用 `127.0.0.1`，不需要配置局域网 IP，也不需要执行 Windows 防火墙命令。

| 调用方向 | 代理端口 | 实际服务端口 |
|---|---:|---:|
| Sender 01 → Scheduler | `18031` | `8003` |
| Sender 01 → Edge（MQTT） | `18831` | `1883` |
| Edge → Scheduler | `18011` | `8003` |
| Scheduler → Edge | `18042` | `8001` |
| Edge → MOMENT 云端 | `18021` | `8004` |
| MOMENT 云端 → Scheduler | `18045` | `8003` |

只有访问这些代理端口的业务流量才会被网络模块施加延迟、抖动、带宽限制和断连。直接访问 `8001`、`8003` 或 `8004` 的业务流量不经过网络模拟。

建议 LLM 服务（`8002`）只被 Edge 在本机直连调用，不经过网络模拟，也不占用上表中的任何代理端口。

---

## 12. Sender 数据回放前的准备

完整系统启动后，才可以使用 Sender 模块回放 MAT 数据。

当前 Sender 模块一次会同时运行三个 Sender，每个 Sender 发送 80 个 50ms 数据包。因此运行 Sender 前需要：

1. 准备三份 MAT 数据文件；
2. 为 `sender_01`、`sender_02`、`sender_03` 分别提供路径；
3. 确认 Sender 的 Scheduler 请求地址已使用对应网络代理端口；
4. 确认上述四项健康检查均通过。

未准备好 MAT 数据时，不要直接执行 Sender 命令。

---

## 13. 停止项目

在 Scheduler、云端、Edge（以及建议 LLM，若已启动）各服务窗口中，分别按：

```text
Ctrl + C
```

然后停止网络模拟器：

```powershell
cd <项目根目录>\cloud_edge_project\internet_service\network_simulator
```

```powershell
docker compose --env-file .env down
```

该操作只停止服务与容器，不会删除 Conda 环境、项目代码或已下载的模型。

---

## 14. 下次启动的最短顺序

环境和模型配置完成后，下次无需重复执行第 2 至第 7 节。只需：

1. 启动 Docker Desktop；
2. 运行 `.\start_project.ps1`（见第 8 节），或按第 9 节手动启动；
3. 完成健康检查；
4. 再回放 Sender 数据。

所有 `$env:` 开头的配置仅对所在 PowerShell 窗口生效。关闭窗口后自动失效，不会修改 Windows 的永久环境变量。
